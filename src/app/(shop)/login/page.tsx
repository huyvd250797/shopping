import { PlaceholderPage } from "@/components/placeholder-page";

export default function CustomerLoginPage() {
  return <PlaceholderPage title="Đăng nhập khách hàng" route="/login" targetVersion="V0.2.0 – Auth & Roles" description="V0.1.0 chỉ bật Admin Auth. Customer signup/login/logout/reset password sẽ làm ở V0.2.0; khách vẫn được xem catalog và về sau có thể checkout guest." />;
}
