import { PlaceholderPage } from "@/components/placeholder-page";

export default function SearchPage() {
  return <PlaceholderPage title="Tìm kiếm sản phẩm" route="/search" targetVersion="V0.4.0 – Home & Search UX" description="Route đã được dựng sẵn. Search, filter, sort và pagination sẽ nối catalog thật ở đúng version roadmap." />;
}
